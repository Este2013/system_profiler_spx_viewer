name: system_profiler_spx_viewer
description: "A desktop viewing app for macOS system profiler (spx) files"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.0-113.2.beta

dependencies:
  flutter:
    sdk: flutter
  xml: ^6.5.0
  file_picker: ^8.1.1
  desktop_drop: ^0.4.4
  provider: ^6.1.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
